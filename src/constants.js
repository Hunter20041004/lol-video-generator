/**
 * Hextech Video Studio · Remotion shared constants
 * -------------------------------------------------------------------------
 * Single source of truth for cross-template tuning values.
 * Keep this file dependency-free (no React / Remotion imports) so it can be
 * imported from any template, sub-component, or test without circular issues.
 */

// ===== Audio mixing =====
// BGM bed-track volume across all 6 templates.
// Shared BGM bed volume. This is the only generated audio layer.
export const DEFAULT_BGM_VOLUME = 0.15;
