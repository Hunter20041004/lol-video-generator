# 已決：發布 QA 的賽制標籤

2026-09-04 使用者核准處理三個發布阻擋。檢查確認 PostMatchReadFrame 固定 BO3，而 postMatchReadBuilder 的 seriesContext 只有比分與實際 gameCount，沒有可靠 best-of 欄位。

- 推薦：標頭改為「賽後判讀 · LCK · 共 5 局」這類實際局數，依既有完整 snapshot 顯示，不推測 BO3／BO5；保留版面與遊戲過程 GAME 標頭。沒有新增來源請求、資料結構或付費依賴。
- 備援：補齊可驗證的原始賽制資料再顯示 BO3／BO5，缺資料時不顯示賽制；範圍較大，需規劃來源查詢與保存相容性。
- 使用者回覆「嗯」，已核准共幾局；隔離分支實作與真實影片驗證通過，audit 服務逾時暫不整合。帳號授權與公開媒體網址獨立處理；實際發布仍需指定影片／平台／帳號／貼文確認。

## 待確認：可用的固定網域

帳號唯讀驗證已確認 IG／Threads 授權過期；目前 callback 與 public media 的兩個 trycloudflare 主機均 ENOTFOUND。固定網址可避免每次臨時隧道更換都需修改平台 callback 登錄；需先確認使用者是否有可管理網域，不自行購買或新增付費服務。備援是臨時網址，但仍需平台重新登錄與存取範圍檢查，不直接把整個工具公開。

# 已決：原有套件安全修補

使用者已核准，fast-uri 3.1.7、browserslist 4.28.8 更新後 audit 0；分支完整閘門通過。目前沒有待拍板事項。

2026-09-03 scan cache 已實作且功能驗證通過，但整合被 npm audit 阻擋。

- 分支與主線皆為 2 high：browserslist <=4.28.6、fast-uri 3.0.0–3.1.5。
- 主方案：針對這兩項原有套件套用最小安全更新，先檢查相依樹及主線未提交 lockfile 差異，再重跑安全／完整品質閘門。不得直接混入或覆蓋主線未提交變更。
- 備援：保留 scan-cache 修復於隔離工作區，暂不合併，原網址仍是舊版。不可降低 audit 門檻或忽略漏洞。
- 使用者已回覆「同意」，核准將上述兩項原有套件的最小安全更新納入本輪。只更新目前 major 內版本及必要相依，重新全測後整合，保留主線既有 lockfile 修改。

## 已決：歷史賽事保存資料的可用期限

2026-09-03，Leaguepedia scan cache 修復接手檢查。

- 現有施工計畫允許限流時回傳七天內的原始 scanId，但 `runPlayerRadarFromSnapshot()` 使用 `readCandidateSnapshot()` 預設一天期限；會造成掃描成功、預覽失敗。
- 建議：僅對已結束日期、完整且符合本次查詢條件的資料，允許七天內在限流時供掃描與預覽使用；保留原始取得時間，明確顯示使用保存資料，不延長所有 scanId 的期限，不改自動發布行為。今天仍以重新查詢為準。
- 備援：保持所有資料一天期限；只減少一天內重複掃描，限流時較舊資料不能救援。
- 代價：七天備援可能不含來源後續更正；一天方案較保守但可用性較差。兩者都不需新增付費服務。
- 使用者已回覆「同意」。採用有限度的七天備援，補齊過期邊界與實際預覽讀取的垂直 TDD，直接施工。
